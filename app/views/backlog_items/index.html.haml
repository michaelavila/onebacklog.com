%h1= "Backlog"

%table.table.table-striped 
  - if @backlog_items.empty?
    No items in the backlog
  - else
    - @backlog_items.each do |backlog_item|
      %tr
        %td
          %span.col-lg-1= backlog_item.position
          %span.col-lg-9= link_to backlog_item.name, backlog_item_path(backlog_item)
