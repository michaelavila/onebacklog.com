%h1= "OneBacklog"

%table.table.table-striped 
  - if @backlog_items.empty?
    No items in the backlog
  - else
    - @backlog_items.each do |backlog_item|
      %tr
        %td
          %span.col-lg-12= link_to backlog_item.name, backlog_item_path(backlog_item)
